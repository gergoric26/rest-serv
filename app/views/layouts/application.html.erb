<!DOCTYPE html>
<html>
<head>
  <title>RestServ</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body class="container">
  
  <div>
    <ul class="nav nav-tabs">
      <li><%= link_to "About", root_path %></li>
      <li><%= link_to "All Resumes", main_all_resumes_path %></li>
      <% if user_signed_in? %>
        <li><%= link_to "My Resumes", user_resumes_path(current_user) %></li>
      <% end %>

      <div class="pull-right user-info">
        
        <% if current_user %>

          <div class="pull-right">
            <%= current_user.email %> <%= link_to "Edit", edit_user_registration_path(current_user) %> | <%= link_to "Sign Out", destroy_user_session_path(current_user), method: :delete %>
          </div>

        <% else %>

          <%= link_to "Sign In", new_user_session_path %> or
          <%= link_to "Sign Up", new_user_registration_path %>
        
        <% end %>
    
      </div>
    
    </ul>
  

    <%= yield %>
  </div>

  
</body>
<!-- <footer class="footer">
    <p class="text-muted">Created by Dean Gergoric</p>
</footer> -->
</html>
